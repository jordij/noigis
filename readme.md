# GIS evaluation of Noises MPA proposals

Generate clipped data:

```
$ python raster_clip.py {path to rasters folder} {path to mask shape} {optional output folder path}
```

Results in the `./output/` folder.